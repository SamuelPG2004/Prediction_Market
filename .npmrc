# Privy declara `permissionless` como peer opcional para Smart Wallets/ERC-4337.
# La fase 1 usa exclusivamente su EOA embebida y viem debe permanecer en 2.55.19.
# Evita que npm intente resolver ese peer opcional incompatible durante builds CI.
legacy-peer-deps=true
